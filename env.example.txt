# Copy this file to `.env.local`.

NEXT_PUBLIC_SITE_URL=https://benrososafaris.co.ke

# Supabase — required for portal + contact form
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Required for server-side admin writes. Without it, the contact form uses the submit_enquiry RPC fallback.
# Find in Supabase Dashboard → Project Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=

# Localization
NEXT_PUBLIC_DEFAULT_LOCALE=en
NEXT_PUBLIC_SUPPORTED_LOCALES=en,sw,fr,de,es,it,zh

# Optional Sentry. Leave disabled until the production project is configured.
NEXT_PUBLIC_SENTRY_DISABLED=true
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_ORG=
NEXT_PUBLIC_SENTRY_PROJECT=

# Optional Docker/standalone output.
BUILD_STANDALONE=

# Outbound email (Benroso cPanel SMTP — mail.benrososafaris.co.ke)
# Set SMTP_PASSWORD in Vercel only. Never commit credentials to git.
SMTP_HOST=mail.benrososafaris.co.ke
SMTP_PORT=587
SMTP_SECURE=false
SMTP_PASSWORD=

# Team notifications: inquiries@ sends to info@ (set true for production)
ENABLE_ENQUIRY_NOTIFICATIONS=true
ENQUIRY_NOTIFICATION_EMAIL=info@benrososafaris.co.ke
ENQUIRY_REPLY_TO=info@benrososafaris.co.ke
# Optional overrides (defaults shown):
# SMTP_ENQUIRY_USER=inquiries@benrososafaris.co.ke
# SMTP_ENQUIRY_FROM=Benroso Safaris <inquiries@benrososafaris.co.ke>

# Guest auto-reply after form submit (no-reply@)
ENABLE_GUEST_ENQUIRY_CONFIRMATION=true
# SMTP_GUEST_USER=no-reply@benrososafaris.co.ke
# SMTP_GUEST_FROM=Benroso Safaris <no-reply@benrososafaris.co.ke>

# Newsletter campaigns (news@)
# SMTP_NEWSLETTER_USER=news@benrososafaris.co.ke
# SMTP_NEWSLETTER_FROM=Benroso Safaris <news@benrososafaris.co.ke>

# Supabase Auth verification emails: configure custom SMTP in Supabase Dashboard
# (Authentication → Email → SMTP) using no-reply@benrososafaris.co.ke and the same host/port.
# Optional override for WhatsApp links in enquiry comms helpers
BENROSO_WHATSAPP_PHONE=+254731201500

# Optional WhatsApp Business API auto-summary (stub until configured)
WHATSAPP_API_ENABLED=false
WHATSAPP_API_TOKEN=
WHATSAPP_NOTIFICATION_PHONE=+254731201500
